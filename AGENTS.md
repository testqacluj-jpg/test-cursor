# Agents

## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) with no application code, build system, or services. The repo currently contains only a `README.md`, a `test.txt`, and an image file (`fff.png`).

- **No package manager or dependencies**: There is no `package.json`, `requirements.txt`, `pyproject.toml`, or any other dependency manifest.
- **No build/lint/test commands**: There are no scripts, Makefiles, or CI configurations to run.
- **No services to start**: There is no backend, frontend, database, or any other service.

If application code is added in the future, update this file with relevant setup, build, lint, test, and run instructions.
