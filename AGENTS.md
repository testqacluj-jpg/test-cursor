# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) with no application code, dependencies, or build system.

- **Contents**: `README.md` (title only) and `test.txt` (test string).
- **No runtime services**: There are no applications, servers, or services to start.
- **No dependencies**: No `package.json`, `requirements.txt`, `pyproject.toml`, `Makefile`, or similar files exist.
- **No tests or linting**: No test frameworks or linters are configured.
- **No build step**: Nothing to compile or bundle.

If application code is added in the future, update this file with setup, build, lint, test, and run instructions.
