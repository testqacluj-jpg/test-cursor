# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal test/placeholder repository (`test-cursor`) containing only `README.md` and `test.txt`. There is:

- No application code or services to run
- No dependencies or package manager configuration
- No build system, linter, or test framework
- No database or external service requirements

The update script is intentionally a no-op (`true`). If the repository evolves to include actual application code, update the script and this section accordingly.
