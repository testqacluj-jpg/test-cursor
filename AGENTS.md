## Cursor Cloud specific instructions

This is a minimal test/scratch repository (`test-cursor`) with no application code, build system, or dependencies.

### Repository contents
- `README.md` — single-line title
- `test.txt` — placeholder text file
- `fff.png` — test image

### Services
There are no services, applications, or executables to run. No dev server, no backend, no frontend.

### Lint / Test / Build
No linter, test framework, or build tool is configured. There are no `package.json`, `Makefile`, `pyproject.toml`, `Cargo.toml`, or similar files.

### Development notes
- No dependency installation is needed.
- No environment variables or secrets are required.
- If application code is added in the future, update this file with setup/run instructions.
