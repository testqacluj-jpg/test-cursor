# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) with no application code, no dependencies, and no services.

### Repository contents
- `README.md` — Project heading only
- `test.txt` — A test text file
- `fff.png` — An image file

### Development environment
- No package manager, build system, or runtime dependencies are required.
- No services need to be started.
- No tests, linting, or build commands exist.
- The update script is a no-op (`true`) since there are no dependencies to install.

### Notes for future agents
- If application code is added in the future, update this file and the VM update script accordingly.
