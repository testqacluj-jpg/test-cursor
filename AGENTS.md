# Agents

## Cursor Cloud specific instructions

This is an empty test/placeholder repository (`test-cursor`). It contains no application code, no dependencies, no services, and no build configuration.

- **No package manager or dependency files** exist (no `package.json`, `requirements.txt`, etc.).
- **No build system** is configured.
- **No services** to start or test.
- **No lint, test, or build commands** are available.

When application code is added to this repository, this file should be updated with relevant development instructions.
