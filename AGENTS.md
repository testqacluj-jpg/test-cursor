## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) with no application code, no dependencies, and no services.

- **No build system**: There are no `package.json`, `Makefile`, `Dockerfile`, or other build configuration files.
- **No source code**: The repo contains only `README.md`, `test.txt`, and an image file.
- **No services to run**: There is no application server, database, or external dependency.
- **No tests or linting**: No test framework or linter is configured.

If application code is added in the future, update this file with relevant build/run/test instructions.
