# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) containing only `README.md` and `test.txt`. There are no applications, services, dependencies, build systems, linters, or test frameworks configured.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Cargo.toml`, or similar.
- **No services to run** — no backend, frontend, or database.
- **No tests or linting** — no test framework or linter is configured.
- **No build step** — nothing to compile or bundle.

If code is added to this repo in the future, update this file with the relevant setup, build, test, and run instructions.
