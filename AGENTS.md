# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal scaffold repository containing only `README.md` and `test.txt`. There are no application services, no dependencies, no build system, and no tests.

- **No package manager or dependencies**: No `package.json`, `requirements.txt`, or similar files exist.
- **No services to run**: There are no backend/frontend servers or databases.
- **No lint/test/build commands**: No tooling is configured.
- **Git is the only tool needed**: All operations are standard git workflows.

If application code is added in the future, update this file with the relevant setup, build, test, and run instructions.
