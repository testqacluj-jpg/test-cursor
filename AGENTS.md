# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal test repository (`test-cursor`) with no application code, dependencies, or services.

### Repository contents
- `README.md` — single-line heading
- `test.txt` — single-line test content

### Development environment
- No package manager, build system, or dependency installation is required.
- No services need to be started.
- No lint, test, or build commands exist.
- Standard git operations work as expected.
